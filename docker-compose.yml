# Деплой на Timeweb Cloud VPS
# Запуск: docker compose up -d

services:
  bot:
    build: .
    container_name: bakery-bot
    restart: unless-stopped
    env_file: .env
    environment:
      - RUN_API=true
      - API_HOST=0.0.0.0
      - API_PORT=8080
    ports:
      - "8080:8080"
